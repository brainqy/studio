# Environment variables for Production (NODE_ENV=production)
# These are loaded when running `npm run build` and `npm run start`
# IMPORTANT: Do NOT commit sensitive credentials directly into this file if it's version controlled.
# Use your deployment platform's environment variable management for secrets.

# Base URL for your deployed Spring Boot backend
NEXT_PUBLIC_API_BASE_URL=https://your-production-api.example.com/api # Replace with your actual production API URL

# Production database connection details (SHOULD BE SET IN DEPLOYMENT ENVIRONMENT, NOT COMMITTED)
# DB_HOST=your_prod_db_host
# DB_PORT=your_prod_db_port
# DB_USER=your_prod_db_user
# DB_PASSWORD=your_prod_db_password
# DB_NAME=resumematch_prod_db

# Example: Ensure feature is enabled in production
# NEXT_PUBLIC_FEATURE_XYZ_ENABLED=true
