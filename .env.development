
NODE_ENV=development

# Base URL for your Spring Boot backend API when running locally
# The data-service.ts will use sample data for job openings in dev mode,
# but this URL would be used for other API calls (e.g., auth) to your Spring Boot backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# Use a development-specific Google API key if possible, or your general one
GOOGLE_API_KEY=YOUR_DEV_GOOGLE_API_KEY_HERE

# JWT Secret for Next.js specific auth if any (e.g., for middleware if not calling external auth)
# This is separate from Spring Boot's JWT secret.
JWT_SECRET=your_dev_jwt_secret_for_nextjs_if_any_local_auth_is_mocked_or_handled_by_nextjs_auth

# Development MySQL Database Connection Details (ensure these match your local MySQL setup)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=resumematch_dev_user
DB_PASSWORD=resumematch_dev_password
DB_NAME=resumematch_dev_db
